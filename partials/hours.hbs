{{!-- <div class="hours-list">  
  {{#with (formatHours this "Monday" "TimeFormatShort" "DateFormatShort") }}
      {{#each this.regularHours}}
        <p><strong>{{this.displayDay}}:</strong>
        {{#if this.isClosed}}Closed{{/if}}
        {{#if this.is247}}Open 24/7{{/if}}
        {{#each this.openIntervals}}
          {{this.start}}-{{this.end}}
        {{/each}}
        </p>
      {{/each}}
  {{/with}}
</div> --}}

<div class="hours-list text-base xl:text-sm">  
  {{#with (formatHours this "Monday" "TimeFormatShort" "DateFormatShort") }}
      {{#each this.regularHours}}
      <div class="grid grid-cols-2 inline-block">
        <div>
          <strong>{{this.displayDay}}:</strong>
        </div>
        <div class="">
          {{#if this.isClosed}}Closed{{/if}}
          {{#if this.is247}}Open 24/7{{/if}}
          {{#each this.openIntervals}}
            {{this.start}}-{{this.end}}
          {{/each}}
        </div>
      </div>
      {{/each}}
  {{/with}}
</div>
